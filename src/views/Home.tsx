import { Typography } from "@mui/material";

import NavBar from "components/NavBar";

const Home = () => {
    return (
        <>
            <NavBar />
            <Typography>Hello!</Typography>
        </>
    );
};

export default Home;
